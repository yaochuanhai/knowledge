"# knowledge" 

1、python -m venv venv

2、.\venv\Scripts\python.exe -m pip install openai

3、.\venv\Scripts\python.exe -m pip install dotenv

安装打包工具
pip install pyinstaller

执行打包命令
pyinstaller --onefile --clean main.py

pip install streamlit

# 确保你已经激活了虚拟环境
pip freeze > requirements.txt
